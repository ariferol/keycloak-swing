# Copy this file to config.properties and fill in your own values.

# Keycloak configuration
keycloak.clientId=sampleClientId

# Confidential client secret (avoid committing real secrets in production)
keycloak.clientSecret=a51f030f49134bcba7958d8faa1eac07(your client secret)
keycloak.redirectUri=http://localhost:8080/callback
keycloak.scope=openid profile email
keycloak.serverUrl=https://keycloak.sample.com
keycloak.realmId=sampleRealmId

# Local embedded HTTP server
local.server.port=8080

# Logging
app.log.timestamp=true
